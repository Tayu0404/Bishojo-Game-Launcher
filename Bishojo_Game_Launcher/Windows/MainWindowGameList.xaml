<UserControl x:Class="Bishojo_Game_Launcher.Windows.MainWindowGameList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Bishojo_Game_Launcher.Windows"
			 xmlns:properties="clr-namespace:Bishojo_Game_Launcher.Properties"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
	<Grid Margin="5, 0, 5, 0">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="350"/>
			<ColumnDefinition Width="10"/>
			<ColumnDefinition Width="*"/>
			</Grid.ColumnDefinitions>
			<Grid Grid.Column="0">
				<Grid.RowDefinitions>
					<RowDefinition Height="40"/>
					<RowDefinition Height="*"/>
				</Grid.RowDefinitions>

				<Border Grid.Row="0" Style="{DynamicResource DefaultBorderStyle}">
					<Grid>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="*"/>
							<ColumnDefinition Width="80"/>
							<ColumnDefinition Width="40"/>
						</Grid.ColumnDefinitions>
						<TextBox Grid.Column="0" FontSize="15" Tag="{x:Static properties:Resources.SearchGameDetaile}" Style="{DynamicResource DefaultTextBoxStyle}"/>
						<Button Grid.Column="1" FontSize="15" Content="{x:Static properties:Resources.Search}" Style="{DynamicResource DefaultButtonStyle}"/>
						<Button Grid.Column="2" FontSize="15" Content="⚙" Style="{DynamicResource DefaultButtonStyle}"/>
					</Grid>
				</Border>

			<ListBox Grid.Row="1" x:Name="GameList" SelectionChanged="GameList_SelectionChanged" Style="{DynamicResource DefaultListBoxStyle}">
				<ListBox.ItemTemplate>
					<DataTemplate>
						<Grid Height="25" x:Name="Hash" Tag="{Binding Hash}">
							<StackPanel Orientation="Horizontal">
								<Image Source="{Binding AppIcon}" Width="25"/>
								<TextBlock Margin="5,0,0,0" VerticalAlignment="Center" Text="{Binding Title}"/>
							</StackPanel>
						</Grid>
					</DataTemplate>
				</ListBox.ItemTemplate>
			</ListBox>
		</Grid>
		<Border Grid.Column="2" BorderBrush="Black" BorderThickness="1">
			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition Height="2*"/>
					<RowDefinition Height="50"/>
					<RowDefinition Height="3*"/>
				</Grid.RowDefinitions>
				<Image Grid.Row="0" Grid.RowSpan="3" x:Name="MainImage" Stretch="UniformToFill"/>
				<Border Grid.Row="1" Opacity="0.8" Style="{DynamicResource DefaultBorderStyle}">
					<Grid Margin="5">
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="5"/>
							<ColumnDefinition Width="100"/>
							<ColumnDefinition Width="*"/>
							<ColumnDefinition Width="40"/>
						</Grid.ColumnDefinitions>
						<Button Grid.Column="1" Opacity="1" FontSize="25" Content="{x:Static properties:Resources.Play}" Style="{DynamicResource BackgroundColerButtonStyle}"/>
						<Button Grid.Column="3" Opacity="1" FontSize="20" Content="⚙" Style="{DynamicResource DefaultButtonStyle}"/>
					</Grid>
				</Border>
				<Grid Grid.Row="2" Opacity="0.8" Style="{DynamicResource DefaultGridStyle}"/>
			</Grid>
		</Border>
	</Grid>
</UserControl>
